{
    "name": "ini-nama-project",
    "version": "1.0.0",
    "description": "Ini deskripsi project nya",
    "type": "module",
    "scripts": {
        "coba": "mocha tests --recursive --timeout 5000 --reporter spec",
        "coba-report-html": "mocha tests --recursive --reporter mocha-simple-html-reporter --reporter-option output=report.html",
        "coba-report-json": "mocha-tests --recursive --reporter json > test-result.json"
    },
    "dependencies": {
        "ajv": "^8.17.1",
        "node-fetch": "^3.3.1"
    },
    "devDependencies": {
        "chai": "^4.3.8",
        "mocha": "^10.2.0",
        "mocha-simple-html-reporter": "^2.0.0"
    }
}
